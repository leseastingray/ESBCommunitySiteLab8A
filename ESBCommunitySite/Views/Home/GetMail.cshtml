@model List<ContactInfo>

@{
    Layout = null;
}
<! DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>MailBox</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />
</head>
<body>
    <div class="panel-body">
    <h2>Mailbox</h2>
        <table class="table table-lg table-striped table-bordered">
            <thead>
                <tr>Recipient</tr>
                <tr>Message Content</tr>
                <tr>Sender</tr>
            </thead>
            <tbody>
                @foreach (ContactInfo cI in Model)
                {
                    <tr>
                        <td class="info">@cI.Recipient</td>
                        <td>@cI.MessageText</td>
                        <td>@cI.MessagePriority</td>
                        <td class="info">@cI.Sender</td>
                        <td>@cI.MessageDate</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div>
        <button asp-action="GetPriorityMail">Order By Priority</button>
        <button asp-action="GetMail">Order By Date</button>
    </div>
</body>
</html>

